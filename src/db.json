//"Database" which has the analysis 
{
    "histogram": {
       "cells": {
            "1":{
                "type": "markdown",
                "content": "this is a histogram cell"
            },
            "2":{
                "type": "code",
                "content": '''hist = library.make_histogram(input_parameters["histogram"]) \
                        px.plot(hist)
                '''
            }
       }
    }

}